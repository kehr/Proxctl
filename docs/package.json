{
  "name": "proxctl-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "../scripts/docs-metadata.sh && vitepress --host 127.0.0.1",
    "build": "../scripts/docs-metadata.sh && vitepress build",
    "preview": "vitepress preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
